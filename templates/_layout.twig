<!DOCTYPE html>
<html lang="{{craft.app.language}}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{siteName}}</title>
    {% do craft.app.view.registerCssFile("@web/css/normalize.css") %}
    {% do craft.app.view.registerCssFile("@web/css/tennos.css?") %}
</head>
<body>
    <header>
        <div class="topHead">
            <a class="logo" href="{{siteUrl}}"> <img  src="{{ url('/images/Logo.svg') }}" alt="LogoTennos"></a>
            <div class="secHead">
                <a href="{{siteUrl}}rackets"><img src="" alt="">Rackets.</a>
            </div>
            <div class="topIcons">
                <a class="fav" href="#"><img src="{{url('/images/heart.svg')}}" alt="favorites"></a>
                <a class="shopIcon" href=""><img src="{{url('/images/shopping-cart.svg')}}" alt="shoppingCart"></a>
            </div>
        </div>
    </header>

     {% block content %} 

    

    {% endblock %}


    <div class="all">
        <a href="{{siteUrl}}rackets">See all.</a>
    </div>


    <footer>
        <a class="footLogo" href="{{siteUrl}}"><img src="{{ url('/images/Logo.svg') }}" alt="Logo"></a>
        <div class="footInfo">
            <a href="{{siteUrl}}rackets">Rackets.</a>
        </div>
        <div class="footIcons">
            <a href="#"><img src="{{ url('/images/mail.svg')}}" alt="mail"></a>
            <a href="#"><img src="{{url('/images/facebook.svg')}}" alt="facebook"></a>
            <a href="#"><img src="{{url('/images/instagram.svg')}}" alt="instagram"></a>
        </div>

    </footer>


</body>

</html>